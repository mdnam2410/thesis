Our development environment requires the execution computer to have Conda installed as the package manager, along with Python 3.10 and pip.
To execute files in the SOURCE folder, we start by running the bash script 'SALIENCY_DISAGREEMENT/SETUP/setup_env.sh'.

Following that, we proceed with model training by executing the 'train.sh' script. However, if you are satisfied with the current pretrained models, we can omit this step.

Then, ???